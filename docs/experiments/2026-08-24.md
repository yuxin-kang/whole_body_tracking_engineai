# 2026-08-24 实验记录

## EXP-20260824-01：右直拳固定靶训练策略

- 对象：`Tracking-Flat-T800-Target-StraightPunchR-v0` / `2026-08-20_21-40-11_straight_punch_R_target_a4000_j8216_0` / Slurm array `8216_0` / `model_29999.pt`
- 对照：纯 tracking 改进版 `Tracking-Flat-T800-Improved-StraightPunchR-v0` / `2026-08-13_22-04-54_straight_punch_R_improved_a4000_j7466_1` / `model_29999.pt`
- 基本参数：seed 42，4096 environments，1×RTX A4000，PPO，训练预算 30000 iterations；数据为 `data/npz/traj_eng_50hz_improved/straight_punch_R_terminal_hold_0p5s.npz`，75 帧、1.50 秒；W&B project `urkl-t800`，run `o8ipxb66`

### 用户 Play 结果

用户在客户端 Play 该策略后报告：“好的 这两个很好”。用户指定该策略可作为比赛使用策略。

### 相对上一版本的关键改动

- 从纯 tracking 任务切换为独立的 `Target-StraightPunchR` 任务，保留右直拳原始动作跟踪，并加入固定靶击打环境。
- 靶子为固定的低摩擦、零反弹 compliant padded wall；靶心 `(0.810, -0.182, 1.297)`，击打末端 `LINK_WRIST_END_R`，接触窗口第 15–19 帧，恢复从第 20 帧开始。
- 新增靶面接触奖励（权重 `0.25`）和击打后恢复稳定奖励（权重 `0.20`）；靶心相对位置仅加入 privileged critic，actor 观测维度保持不变。
- 保持 native clip、从第 0 帧播放、末尾不重新采样；关闭 tracking 误差提前终止，仅保留 timeout 和根节点高度摔倒终止。

### 证据

- 训练输出：`logs/rsl_rl/t800_flat/2026-08-20_21-40-11_straight_punch_R_target_a4000_j8216_0/`
- 最终 checkpoint：`model_29999.pt`，6,608,626 bytes，SHA256 `b5f059fba993905f1f782b1b720be7b56944c588815c9177a57f8d6b988475a4`
- W&B：[o8ipxb66](https://wandb.ai/gkmbgm-northeastern-university/urkl-t800/runs/o8ipxb66)
- 训练末尾 TensorBoard（step `29999`）：mean episode length `75.0000`；mean reward `9.8812`；target contact `0.0164`；target recovery `0.1101`；anchor position error `0.0462`；anchor rotation error `0.0355`；body position error `0.0150`；body rotation error `0.0487`；joint position error `0.1978`。
- 任务/靶子实现：`source/whole_body_tracking/whole_body_tracking/tasks/tracking/config/t800/__init__.py`、`flat_env_cfg.py`；训练脚本：`scripts/slurm/train_target_right_punch_front_kick_a4000_array.sbatch`。

## EXP-20260824-02：正蹬固定靶训练策略

- 对象：`Tracking-Flat-T800-Target-LeftFrontKick-v0` / `2026-08-20_21-45-10_left_front_kick_002_target_a4000_j8218_1` / Slurm array `8218_1` / `model_29999.pt`
- 对照：完整 episode 高度摔倒终止版 `Tracking-Flat-T800-Baseline-LeftFrontKick-CompleteEpisode-HeightFallOnly-v0` / `2026-08-13_09-55-57_left_front_kick_002_complete_episode_heightfall_a4000_j7440` / `model_20500.pt`（`EXP-20260813-01`）
- 基本参数：seed 42，4096 environments，1×RTX A4000，PPO，训练预算 30000 iterations；数据为 `data/npz/traj_eng_50hz_episode_complete/left_front_kick_002_terminal_hold_1s.npz`，179 帧、3.58 秒；W&B project `urkl-t800`，run `glupt6at`

### 用户 Play 结果

用户在客户端 Play 该策略后报告：“好的 这两个很好”。用户指定该策略可作为比赛使用策略。

### 相对上一版本的关键改动

- 从 tracking-only 任务切换为独立的 `Target-LeftFrontKick` 任务，保留完整正蹬动作和恢复段，并加入固定靶击打环境。
- 靶子为固定的低摩擦、零反弹 compliant padded wall；靶心 `(1.821, 0.034, 1.135)`，击打末端 `LINK_ANKLE_ROLL_L`，接触窗口第 44–48 帧，恢复从第 49 帧开始。
- 新增靶面接触奖励（权重 `0.25`）和击打后恢复稳定奖励（权重 `0.20`）；靶心相对位置仅加入 privileged critic，actor 观测维度保持不变。
- 保持 179 帧 native clip、从第 0 帧播放、末尾不重新采样；保留完整动作中的有效姿态变化，仅保留 timeout 和根节点高度摔倒终止。

### 证据

- 训练输出：`logs/rsl_rl/t800_flat/2026-08-20_21-45-10_left_front_kick_002_target_a4000_j8218_1/`
- 最终 checkpoint：`model_29999.pt`，6,608,626 bytes，SHA256 `8b304b51faa67026a801f094749cce36444194d14ad65b67cfbed232325dd310`
- W&B：[glupt6at](https://wandb.ai/gkmbgm-northeastern-university/urkl-t800/runs/glupt6at)
- 训练末尾 TensorBoard（step `29999`）：mean episode length `179.0000`；mean reward `15.7496`；target contact `0.0051`；target recovery `0.1118`；anchor position error `0.0667`；anchor rotation error `0.0330`；body position error `0.0139`；body rotation error `0.0454`；joint position error `0.1800`；fall-height termination `0.0000`。
- 任务/靶子实现：`source/whole_body_tracking/whole_body_tracking/tasks/tracking/config/t800/__init__.py`、`flat_env_cfg.py`；训练脚本：`scripts/slurm/train_target_right_punch_front_kick_a4000_array.sbatch`。
